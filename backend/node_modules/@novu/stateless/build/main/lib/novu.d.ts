/// <reference types="node" />
import { EventEmitter } from 'events';
import { INovuConfig } from './novu.interface';
import { IEmailProvider, ISmsProvider, IChatProvider, IPushProvider } from './provider/provider.interface';
import { ITemplate, ITriggerPayload } from './template/template.interface';
import { ITheme } from './theme/theme.interface';
export declare class NovuStateless extends EventEmitter {
    private readonly templateStore;
    private readonly providerStore;
    private readonly themeStore;
    private readonly config;
    private readonly contentEngine;
    constructor(config?: INovuConfig);
    registerTheme(id: string, theme: ITheme): Promise<ITheme>;
    setDefaultTheme(themeId: string): Promise<void>;
    registerTemplate(template: ITemplate): Promise<ITemplate>;
    registerProvider(provider: IEmailProvider | ISmsProvider | IChatProvider | IPushProvider): any;
    registerProvider(providerId: string, provider: IEmailProvider | ISmsProvider | IChatProvider | IPushProvider): any;
    getProviderByInternalId(providerId: string): Promise<IEmailProvider | ISmsProvider | IChatProvider | IPushProvider>;
    trigger(eventId: string, data: ITriggerPayload): Promise<void>;
}
//# sourceMappingURL=novu.d.ts.map