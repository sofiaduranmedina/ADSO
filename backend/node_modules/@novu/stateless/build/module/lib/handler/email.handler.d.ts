import { IContentEngine } from '../content/content.engine';
import { IEmailProvider } from '../provider/provider.interface';
import { IMessage, ITriggerPayload } from '../template/template.interface';
import { ITheme } from '../theme/theme.interface';
export declare class EmailHandler {
    private message;
    private provider;
    private theme?;
    private readonly contentEngine;
    constructor(message: IMessage, provider: IEmailProvider, theme?: ITheme, contentEngine?: IContentEngine);
    send(data: ITriggerPayload): Promise<import("../provider/provider.interface").ISendMessageSuccessResponse>;
}
//# sourceMappingURL=email.handler.d.ts.map