/// <reference types="node" />
import { EventEmitter } from 'events';
import { IContentEngine } from '../content/content.engine';
import { INovuConfig } from '../novu.interface';
import { ProviderStore } from '../provider/provider.store';
import { IMessage, ITemplate, ITriggerPayload } from '../template/template.interface';
import { TemplateStore } from '../template/template.store';
import { ThemeStore } from '../theme/theme.store';
export declare class TriggerEngine {
    private templateStore;
    private providerStore;
    private themeStore;
    private contentEngine;
    private config;
    private eventEmitter;
    constructor(templateStore: TemplateStore, providerStore: ProviderStore, themeStore: ThemeStore, contentEngine: IContentEngine, config: INovuConfig, eventEmitter: EventEmitter);
    trigger(eventId: string, data: ITriggerPayload): Promise<void>;
    processTemplateMessage(template: ITemplate, message: IMessage, data: ITriggerPayload): Promise<void>;
    private getMissingVariables;
    private extractMessageVariables;
    private validate;
}
//# sourceMappingURL=trigger.engine.d.ts.map